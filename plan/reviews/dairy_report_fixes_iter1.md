## Verdict: ISSUES_FOUND
## Iteration: 1 of 3

Reviewed against the latest report `backend/decarb/runs/GOLDEN_DAIRY_5MW_20260505T155310Z.md` and code at HEAD `6409f67` (Wire optimise_investment_pathway into render_report §4). Branch `feature/dairy-report-fixes` is currently identical to `main`; this iter-1 review documents the baseline issues the Builder must address.

Engine regression baseline: `216 passed in 46.94s` (decarb/engine/tests). No new HIGH-severity warnings vs main (none added since no commits yet).

## Issues remaining
- [A] **FAIL.** §1 line 24: "Scope 1 reduction of 6,951 → 5,096 (location-based Scope 2 **rises** from 1,900 to 1,223 …)" — the verb says rises, the numbers say falls. Worse, the numbers are physically wrong. Re-derived from §4.4: HP_output 5,960 MWh / weighted_COP 3.09 ≈ 1,929 MWh elec; EB_output 6,053 MWh / 0.99 ≈ 6,114 MWh elec; total new electricity ≈ 8,043 MWh × 0.152 kgCO₂e/kWh (NESO 2026) ≈ +1,222 tCO₂e. Expected post-dispatch Scope 2 (location) ≈ 1,900 + 1,222 ≈ 3,122 tCO₂e/yr, not 1,223. The same wrong figure propagates to §5.3 (1,900 → 1,223) and the §1 totals (6,318 vs corrected ≈ 8,218). Sign error in dispatch.scope2_location accounting — likely subtracting electrified-load emissions instead of adding them.
- [B] **FAIL.** screen.py defines `sink_temp_c` and the dispatch filter post-hoc removes infeasible end-uses (warnings emitted as `hp_sink_too_cold_for_end_use` + `hp_inactive_no_compatible_end_use` for whr_500 in all three pathways and for hp_high_1000), but the *pathway optimiser still inserts whr_500 (£150k, sink 70°C) into Conservative, Balanced, and Aggressive*. After the dispatch filter its `effective serves_end_uses` is empty — i.e. all three named pathways carry a £150k asset that delivers zero kWh. The screen-time gate must be raised from advisory to hard exclusion before pathway construction (or, equivalently, `optimise_investment_pathway` must drop techs whose post-LMTD effective serves is empty against the site's end-uses).
- [C] **FAIL.** §4.4 dispatch is `hp_1=1000 kW + eb_1=4000 kW + tes_1=8000 kWh` — does not match any §4.1 pathway. Conservative has eb_2000; Balanced has *no* EB; Aggressive has eb_4000 but a 2000 kW HP. Line 269 admits "year 1 of the chosen pathway is similar in shape" and line 340 caveat says "hand-specified rather than optimised". §1 and §5.3 numbers (5,096 / 1,223 / 6,318) come from this orphan dispatch, not from any reported pathway. Either (i) regenerate §4.4 from Balanced year-1, or (ii) explicitly label §4.4 as illustrative and quote Balanced year-1 numbers in §1 / §5.3.
- [D] **FAIL.** Balanced selects `hp_mid_1000 + tes_8000 + whr_500` (no EB); §4.1 has no commentary defending TES-without-EB economics. The required regression test "TES NPV without EB bounded by HP-only charge/discharge" does not exist in `backend/decarb/engine/tests/test_pathway.py` (grep for `TES.*NPV.*without|tes_npv|HP-only.*charge` returns nothing). Either drop TES from this configuration or add the bounding test + §4.1 commentary.
- [E] **FAIL.** Balanced annual O&M = £3,000 (line 183). Provenance row 19 declares O&M fractions HP=0.025, TES=0.015, WHR (likely ~0.015). Active capex (gross) = HP £800k + TES £320k = £1,120k (WHR inactive after filter). Lower-bound O&M ≥ 800k×0.025 + 320k×0.015 = £20k + £4.8k ≈ **£24,800/yr**. Reported £3k is an order of magnitude low — looks like a units bug (only WHR being summed, or year-0 phasing applied to a year-1 figure). Conservative also looks suspect at £23k for a stack including a 1 MW HP + 2 MW EB + TES + WHR (lower bound ≈ £29k–£35k).
- [F] **FAIL.** §4.3 still emits `[advisory/balanced_underperforms_conservative_under_v0_defaults]` (line 261). Under the carbon+grant overlay shown in §4.1, Balanced does have higher NPV than Conservative (£377k vs −£74k), but **lower year-15 carbon reduction** (19.4% vs 25.0%) — so the original failure mode (Balanced not "highest-reduction-with-positive-NPV") still holds. The §4.2 NPV frontier rank 4 (NPV £112k, capex £1.449M, 32.5% reduction) dominates Balanced on carbon at still-positive NPV. The Balanced selection rule must be re-derived to pick highest-reduction-positive-NPV (or the advisory removed and the inversion explicitly defended).
- [G] **FAIL.** §9 contains 4 generic placeholders (e.g. "Confirm the stack is a defensible starting point"). Required form is "Senior to confirm: <specific>. If <alt>, <impact> changes from X to Y." None of the four bullets have the X-to-Y impact line.
- [H] **FAIL.** §8.1 "Engine-flagged warnings" is empty (lines 358–361 are three blank lines). §4.3 has 13 warnings — at minimum the high-severity ones (whr_500 inactive across all pathways, hp_high_1000 inactive) and the advisory inversions should propagate here.
- [I] **PARTIAL FAIL.**
  - §3.1 still reads as concatenated debug strings: "Thermodynamic: …. Capacity: …. Compressor: …. Reference: …. Regulatory: ……" with double full-stops at every join (lines 94, 102, 107). Needs prose rendering.
  - §4 line 138 typo "evaluated through 15-year dispatch × dispatch" (should be "15-year dispatch" or similar) — present.
  - §3 says "3 of 7" candidates retained (line 22) — the methodology references a longlist of 16 (per the checklist). Either §3 must reference the 16-candidate longlist explicitly or the methodology number must be reconciled.
- [J] **FAIL.** §3.1 line 94 notes "HP covers 1287 kW avg demand" but Balanced/Conservative install hp_mid_1000 (1000 kW). No defence in §4.1 commentary, no §9 senior-decision bullet on undersizing. With must_keep_steam_backup retained gas, peaks fall to gas — that argument needs to be stated explicitly, not implied.

## Issues newly introduced
- None (no Builder commits on this branch yet).

## Recommended next iteration prompt for Builder
Address items A–J in priority order:

1. **A (highest, blocker).** Find the Scope 2 sign error in `simulate_site_dispatch` (or wherever the post-dispatch carbon roll-up lives). Post-dispatch Scope 2 (loc) must equal `baseline_scope2 + new_grid_kwh × grid_intensity_for_year(year)`, where new_grid_kwh = HP_output / weighted_COP + EB_output / 0.99. Add a regression test: `test_dispatch_scope2_increases_with_electrification`. Re-emit §1, §5.3, §2.5-derived totals. Fix the §1 narrative verb to match the (now correct, rising) Scope 2 number.
2. **B.** In `optimise_investment_pathway`, after computing each candidate's `effective_serves_end_uses` (post-LMTD-margin filter), drop any tech whose effective set is empty against the site's end-uses *before* costing. Verify whr_500 (sink 70°C) is no longer in any pathway with hot_water at 85°C + 5K. Add test: `test_pathway_excludes_thermodynamically_inactive_tech`.
3. **C.** Either regenerate §4.4 from Balanced year-1 dispatch, or label §4.4 illustrative and route §1 / §5.3 narrative numbers through the Balanced-pathway year-1 dispatch result.
4. **E.** Fix Balanced annual O&M roll-up (sum over active capex_i × om_fraction_i for the pathway's active stack, not the inactive-included stack and not WHR-only). Add an invariant test: `assert pathway.annual_om_y1 >= sum(active_capex_i * om_fraction_i)` with strict equality at year 1.
5. **F.** Re-derive Balanced as the highest-Scope1+2-reduction pathway with NPV > 0 under the appraisal overlay (carbon price + grant). Drop the `balanced_underperforms_conservative` advisory once the selection rule produces the right answer.
6. **D.** Add the `test_tes_npv_bounded_without_eb` test (TES contribution ≤ HP-only charge-discharge arbitrage; if not, optimiser must drop TES from that candidate). If the test fails for current Balanced, either fix the optimiser to drop TES or add §4.1 commentary defending its inclusion.
7. **H.** Populate §8.1 from the union of pathway warnings (high-severity first).
8. **G.** Rewrite §9 with ≥4 specific decisions in "Senior to confirm: …. If <alt>, <impact> changes from X to Y." form. Suggested topics: HP undersizing (J), TES inclusion (D), retained gas backup ceiling on year-15 reduction, IETF grant eligibility, capex envelope ±30%.
9. **I.** Render §3.1 thermodynamic / capacity / compressor / regulatory notes as paragraph prose (single full-stop joins, not "….."). Remove "× dispatch" typo at §4 line 138. Reference the 16-candidate longlist explicitly in §3 (or correct the methodology to "7 candidates" if 16 is stale).
10. **J.** Add HP-undersizing defence: explicit statement in §4.1 Balanced commentary that 1000 kW HP serves base + retained gas takes peaks, with a §9 senior-decision bullet on whether to upsize.

After Builder commits iter-2, re-regenerate the dairy report (`python -m decarb.runners.golden_dairy_5mw` or equivalent) and re-run `pytest decarb/engine/tests` — must remain ≥216 passing, with new tests added per A, B, D, E.
